@using ContentModels = Umbraco.Web.PublishedContentModels;

@inherits UmbracoViewPage<ContentModels.BlockList>

@{ Layout = "TopLevel.cshtml";}


<div class="blockList">
    <div class="grid-x small-up-1 medium-up-2 large-up-4">
        @*@foreach (IPublishedContent page in ipCurrentPg.Children.Where(x => x.IsVisible()))*@
        @foreach (IPublishedContent page in Model.Children.Where(x => x.IsVisible()))
        {
                @Html.Partial("Partials/Generic/BlockItem", page)
        }
    </div>
</div>