@using Models;
@inherits UmbracoViewPage
@{ Layout = "TopLevel.cshtml"; }



@{ 
    //Redirect to parent node
    UmbracoHelper uHelper = new UmbracoHelper(UmbracoContext.Current);
    Response.Redirect(uHelper.TypedContent(UmbracoContext.Current.PageId).Parent.Url);
}