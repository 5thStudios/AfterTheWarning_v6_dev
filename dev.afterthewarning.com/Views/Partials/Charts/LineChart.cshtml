@using Models;
@inherits UmbracoViewPage<Models.StatsByAge>



<div class="grid-x grid-margin-x grid-margin-y">
    <div class="cell text-center">
        <h4>Average Experience Type by Age</h4>
        <h5 style="margin-top:0;">Values are percentages from 0 - 100%</h5>
        <br />
        @*<img alt="" src="~/images/temp/lineChart.jpg" />*@
        <canvas id="canvasAvgExpByAge"></canvas>
    </div>
</div>



<script>
    $(window).on("load", function() {
        var AgeRanges = @Html.Raw(Model.jsonAgeRange);
        var config = {
            type: 'line',
            data: {
                labels: AgeRanges,
                datasets: [
                    @foreach(ChartDataset chart in Model.LstChartData)
                    {

                       @: {
                       @:      label: '@chart.Label',
                       @:      backgroundColor: '@chart.Color',
                       @:      borderColor: '@chart.Color',
                       @:      borderWidth: 2,
                       @:      data: @chart.JsonData,
                       @:      fill: false
                       @:  },
                    }
                ]
            },
            options: {
                steppedLine: false,
                responsive: true,
                legend: {
                    position: 'top'
                },
                title: {
                    display: false,
                    text: ''
                },
                tooltips: {
                    mode: 'index',
                    intersect: false,
                },
                hover: {
                    mode: 'nearest',
                    intersect: true
                },
                scales: {
                    xAxes: [{
                        display: true,
                        scaleLabel: {
                            display: true,
                            labelString: 'Age Range'
                        }
                    }],
                    yAxes: [{
                        display: true,
                        scaleLabel: {
                            display: true,
                            labelString: 'Average Percentage'
                        }
                    }]
                }
            }
        };

        //Run canvas chart
        var ctx = document.getElementById('canvasAvgExpByAge').getContext('2d');
        window.myLine = new Chart(ctx, config);
    });
</script>